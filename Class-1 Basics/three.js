import os from 'os';//ES6 (to enable es6 features in node js we need to install node modules by using cmd npm init -y)
//const os=require('os')//ES5 import and export methods ..
console.log(os.homedir());
console.log(os.type());
console.log(os.arch());
console.log(os.release());
//console.log(os.cpus());
 console.log(os.totalmem());
 console.log(os.freemem());
console.log(os.platform());
